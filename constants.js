const IP =  "localhost";
const PORT = 50541;
const PLAYER_NAME = "LEE";

const MOVEMENTS = {
  w: 'up',
  a: 'left',
  s: 'down',
  d: 'right'
}

const MESSAGES = {
  t: 'totally tubular',
  r: 'radical, bro'
}

module.exports = {
  IP,
  PORT,
  PLAYER_NAME,
  MOVEMENTS,
  MESSAGES
};